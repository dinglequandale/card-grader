# Intentionally minimal. The live video pipeline uses only `phase2.centering`
# (imported directly as `from phase2.centering import measure_centering`), so the
# package doesn't eagerly import the other phase2 detectors here.
